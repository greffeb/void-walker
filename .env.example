# Feedback endpoint (Google Apps Script web app URL)
# Deploy the script from docs/FEEDBACK_SETUP.md, then paste the /exec URL here.
# Leave empty to disable feedback submission (game still works, no crash).
VITE_FEEDBACK_ENDPOINT=
